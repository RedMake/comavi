@{
    Layout = "~/Views/Shared/_Layout_Externo.cshtml";
    ViewData["Title"] = "Acceso Denegado - Sistema Administrativo";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">Acceso Denegado</h4>
            </div>
            <div class="card-body text-center">
                <i class="fas fa-exclamation-triangle fa-5x text-danger mb-3"></i>
                <h5>No tiene permisos para acceder a esta página</h5>
                <p>Por favor contacte al administrador si considera que debería tener acceso.</p>

                <div class="mt-4">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Volver al Inicio</a>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {

}

